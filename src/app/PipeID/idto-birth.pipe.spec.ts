import { IDtoBirthPipe } from './idto-birth.pipe';

describe('IDtoBirthPipe', () => {
  it('create an instance', () => {
    const pipe = new IDtoBirthPipe();
    expect(pipe).toBeTruthy();
  });
});
